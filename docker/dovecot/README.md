# Dovecot configuration planned for G.2.
# Will use official dovecot image with:
# - Maildir volume shared with api service
# - Virtual users generated by sync-imap-users
# - IMAP on port 143, localhost-bound by default
#
# See docs/docker-base.md for status.
